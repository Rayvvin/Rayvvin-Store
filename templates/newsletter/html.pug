doctype html
html
  head
    meta(charset="UTF-8")
    title Latest News from #{companyName}
    style.
      body {
        font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        background-color: #f9f9f9;
        margin: 0;
        padding: 20px;
      }
      .container {
        max-width: 600px;
        margin: auto;
        background-color: #ffffff;
        padding: 20px;
        border-radius: 10px;
      }
      h2 {
        color: #222;
      }
      p {
        color: #666;
        line-height: 1.6;
      }
  body
    .container
      h2 📣 #{title}
      p #{body}
      p Regards,  
      p The #{companyName} Team
